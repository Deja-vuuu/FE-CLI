/*!
 * Copyright 2019 acrazing <joking.young@gmail.com>. All rights reserved.
 * @since 2019-02-18 13:28:42
 */
import { __read, __spread } from "tslib";
/**
 * remove item in array, find it first
 * @param input
 * @param item
 * @param replace
 */
export function splice(input, item) {
    var replace = [];
    for (var _i = 2; _i < arguments.length; _i++) {
        replace[_i - 2] = arguments[_i];
    }
    var index = input.indexOf(item);
    if (index > -1) {
        input.splice.apply(input, __spread([index, 1], replace));
    }
    return index;
}
/**
 * get element of array by index, allow out of range [0, array.length)
 * @param input
 * @param index
 */
export function seek(input, index) {
    index = index % input.length;
    index = index < 0 ? input.length + index : index;
    return input[index];
}
//# sourceMappingURL=array.js.map