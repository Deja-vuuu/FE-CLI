/*!
 *
 * Copyright 2017 - acrazing
 *
 * @author acrazing joking.young@gmail.com
 * @since 2017-12-15 13:35:11
 * @version 1.0.0
 * @desc as-map.ts
 */
/**
 * create a Record according to input type, cases can get in spec file
 * @param source
 * @param key
 */
export function asMap(source, key) {
    if (!source) {
        return {};
    }
    var map = {};
    if (!key) {
        source.forEach(function (item) {
            map[item] = item;
        });
    }
    else {
        source.forEach(function (item) {
            map[item[key]] = item;
        });
    }
    return map;
}
//# sourceMappingURL=as-map.js.map