/*!
 *
 * Copyright 2017 - acrazing
 *
 * @author acrazing joking.young@gmail.com
 * @since 2017-12-15 12:29:38
 * @version 1.0.0
 * @desc map.ts
 */
import { __assign } from 'tslib';
import { ANY } from './consts';
/**
 * create a hashed table, optimized for v8 hidden class
 * @param initial
 */
export function createMap(initial) {
    var root = {};
    root['__'] = ANY;
    delete root['__'];
    __assign(root, initial);
    return root;
}
/**
 * get typed values of object
 * @param host
 * @param owned
 */
export function values(host, owned) {
    if (owned === void 0) { owned = true; }
    if (owned) {
        return Object.keys(host).map(function (k) { return host[k]; });
    }
    else {
        var values_1 = [];
        for (var key in host) {
            values_1.push(host[key]);
        }
        return values_1;
    }
}
//# sourceMappingURL=map.js.map