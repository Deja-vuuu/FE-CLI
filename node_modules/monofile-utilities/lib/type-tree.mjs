/*!
 * Copyright 2019 acrazing <joking.young@gmail.com>. All rights reserved.
 * @since 2019-02-16 11:06:04
 */
export function createTypeTree(prefix, tree) {
    if (prefix.charAt(prefix.length - 1) !== '.') {
        prefix += '.';
    }
    for (var key in tree) {
        if (tree.hasOwnProperty(key)) {
            var value = tree[key];
            if (typeof value === 'string') {
                tree[key] = prefix + (value || key);
            }
            else {
                createTypeTree(prefix + key + '.', value);
            }
        }
    }
    return tree;
}
//# sourceMappingURL=type-tree.js.map