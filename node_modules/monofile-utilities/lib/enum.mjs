/*!
 * Copyright 2019 acrazing <joking.young@gmail.com>. All rights reserved.
 * @since 2019-02-16 10:39:41
 *
 * create typed enums with labels & values group
 */
import { values } from './map';
export function enumValues(Host) {
    return values(Host).filter(function (value) {
        return typeof Host[value] !== 'number';
    });
}
export function enumKeys(Host) {
    return Object.keys(Host).filter(function (key) {
        return typeof Host[Host[key]] !== 'number';
    });
}
export function Enum(input) {
    var host = {
        labels: {},
        values: [],
    };
    for (var key in input) {
        host.labels[key] = input[key] || key;
        host.values.push((host[key] = key));
    }
    return host;
}
//# sourceMappingURL=enum.js.map